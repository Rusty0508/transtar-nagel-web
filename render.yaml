services:
  - type: web
    name: transtar-nagel-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web_app && python app.py
    envVars:
      - key: PORT
        value: 10000